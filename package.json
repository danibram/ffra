{
    "scripts": {
        "postinstall": "lerna bootstrap",
        "dev": "lerna run --scope @ffra/* --parallel watch:build",
        "clean": "rm -rf packages/*/build && rm -rf packages/*/node_modules    "
    },
    "devDependencies": {
        "lerna": "^3.21.0"
    },
    "name": "ffra"
}
